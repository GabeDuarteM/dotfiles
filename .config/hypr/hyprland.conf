# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/Configuring-Hyprland/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
source = ./hyprland/machine-specific/monitors.conf
monitor = , preferred, auto, 1 # Recommended rule for quickly plugging in random monitors

###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = footclient
$fileManager = dolphin
# $menu = ulauncher-toggle
$menu =  vicinae vicinae://toggle
$screenshotCopy = $HOME/.local/bin/takeScreenshot.sh
$screenshotCopyQuick = grimblast --notify copy area
$screenshotEdit = grimblast --notify --freeze edit area
$screenshotSave = grimblast --notify --freeze copysave area "$HOME/Pictures/Screenshots/$(date +'%Y-%m-%d_%H-%M-%S').png"
source = ./hyprland/machine-specific/programs.conf


#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

exec-once = exec uwsm finalize
# exec-once = uwsm app -t service -- /usr/bin/hyprpanel
exec-once = uwsm app -t service -- /usr/lib/pam_kwallet_init
# Change wallpaper every 15 minutes (900 seconds)
exec-once = uwsm app -t service -- $HOME/.config/hypr/run-in-interval.sh 900 $HOME/.config/hypr/change-wallpaper.sh
exec-once = uwsm app -t service -- foot --server
exec-once = uwsm app -t service -- caelestia resizer -d

source = ./hyprland/machine-specific/autostart.conf

#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = XCURSOR_SIZE,24
env = XCURSOR_THEME,Bibata-Modern-Classic # Fallback for apps not supporting server-side cursors
env = HYPRCURSOR_SIZE,24
env = HYPRCURSOR_THEME,HyprBibataModernClassicSVG

# Fixes default applications broken on Dolphin
# https://wiki.archlinux.org/title/Dolphin#Default_applications_have_disappeared_while_running_under_a_window_manager
env = XDG_MENU_PREFIX,arch-

# Theming QT
env = QT_QPA_PLATFORM,wayland;xcb
# env = QT_QPA_PLATFORM,xcb
env = QT_QPA_PLATFORMTHEME,qt6ct
# env = QT_QPA_PLATFORMTHEME,qt5ct
env = QT_WAYLAND_DISABLE_WINDOWDECORATION,1
env = QT_AUTO_SCREEN_SCALE_FACTOR,1

source = ./hyprland/machine-specific/environment.conf


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general

general {
    gaps_in = 5
    gaps_out = 20

    border_size = 2

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 10
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 8
        passes = 3

        vibrancy = 0.1696
        new_optimizations = true
        ignore_opacity = true
    }

}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc { 
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
    middle_click_paste = false
}

# debug {
#     disable_logs = false
# }

#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us
    kb_model =
    kb_options =
    kb_rules =

    # xset r rate 200 40
    repeat_rate = 40
    repeat_delay = 200

    follow_mouse = 1

    sensitivity = -0.5 # -1.0 - 1.0, 0 means no modification.
    accel_profile = flat

    touchpad {
        natural_scroll = true
    }
}

source = ./hyprland/machine-specific/input.conf

# https://wiki.hyprland.org/Configuring/Variables/#gestures
# gestures {
#     workspace_swipe = false
# }

###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, Return, exec, uwsm app -- $terminal
bind = $mainMod, q, killactive
bind = $mainMod CTRL, q, exec, uwsm app -- hyprctl kill
bind = $mainMod, N, exec, uwsm app -- $fileManager
bind = $mainMod SHIFT, space, togglefloating,
bind = $mainMod, d, exec, uwsm app -- $menu
bind = $mainMod, Period, exec, uwsm app -- vicinae vicinae://extensions/vicinae/core/search-emojis
bind = $mainMod, w, exec, uwsm app -- $browser
bind = $mainMod, P, pin, # dwindle
bind = $mainMod, o, togglesplit, # dwindle
bind = $mainMod, F, fullscreenstate, 2, 1
bind = $mainMod SHIFT, F, fullscreen, 1
bind = , Print, exec, uwsm app -- $screenshotSave
bind = CTRL, Print, exec, uwsm app -- $screenshotCopy
bind = SHIFT, Print, exec, uwsm app -- $screenshotCopyQuick
bind = CTRL SHIFT, Print, exec, uwsm app -- $screenshotEdit
bind = $mainMod, v, exec, uwsm app -- pavucontrol
bind = $mainMod CTRL, L, exec, uwsm app -- loginctl lock-session

# Move focus with mainMod + hjkl
bind = $mainMod, h, movefocus, l
bind = $mainMod, j, movefocus, d
bind = $mainMod, k, movefocus, u
bind = $mainMod, l, movefocus, r

# Switch main workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Switch alt workspaces with mainMod + [0-9]
bind = ALT, 1, workspace, 11
bind = ALT, 2, workspace, 12
bind = ALT, 3, workspace, 13
bind = ALT, 4, workspace, 14
bind = ALT, 5, workspace, 15
bind = ALT, 6, workspace, 16
bind = ALT, 7, workspace, 17
bind = ALT, 8, workspace, 18
bind = ALT, 9, workspace, 19
bind = ALT, 0, workspace, 20

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

bind = ALT SHIFT, 1, movetoworkspace, 11
bind = ALT SHIFT, 2, movetoworkspace, 12
bind = ALT SHIFT, 3, movetoworkspace, 13
bind = ALT SHIFT, 4, movetoworkspace, 14
bind = ALT SHIFT, 5, movetoworkspace, 15
bind = ALT SHIFT, 6, movetoworkspace, 16
bind = ALT SHIFT, 7, movetoworkspace, 17
bind = ALT SHIFT, 8, movetoworkspace, 18
bind = ALT SHIFT, 9, movetoworkspace, 19
bind = ALT SHIFT, 0, movetoworkspace, 20

# Move active window to specific window
bind = $mainMod SHIFT, h, movewindow, l
bind = $mainMod SHIFT, j, movewindow, d
bind = $mainMod SHIFT, k, movewindow, u
bind = $mainMod SHIFT, l, movewindow, r

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

bind = CTRL, space, exec, pkill -USR2 -n handy

# Scroll through existing workspaces with mainMod + scroll
# bind = $mainMod, mouse_down, workspace, e+1
# bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
# bindm = $mainMod, HOME, movewindow
# bindm = , mouse:273, movewindow
# bindsm = , mouse:274, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl s 10%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl s 10%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous


# ## Caelestia
# exec = hyprctl dispatch submap global
# submap = global
# Launcher

# bindi = Super CTRL, O, global, caelestia:launcher
# bindin = Super, catchall, global, caelestia:launcherInterrupt
# bindin = Super, mouse:272, global, caelestia:launcherInterrupt
# bindin = Super, mouse:273, global, caelestia:launcherInterrupt
# bindin = Super, mouse:274, global, caelestia:launcherInterrupt
# bindin = Super, mouse:275, global, caelestia:launcherInterrupt
# bindin = Super, mouse:276, global, caelestia:launcherInterrupt
# bindin = Super, mouse:277, global, caelestia:launcherInterrupt
# bindin = Super, mouse_up, global, caelestia:launcherInterrupt
# bindin = Super, mouse_down, global, caelestia:launcherInterrupt

# Misc
bind = Ctrl+Alt, Delete, global, caelestia:session
bind = Ctrl Super, K, global, caelestia:showall

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule v1
# windowrule = float, ^(kitty)$

# Example windowrule v2
# windowrule = float,class:^(kitty)$,title:^(kitty)$
# windowrule = center 1, floating:1, class:negative:steam

# windowrule = suppressevent maximize, class:.* # You'll probably like this.
windowrule {
  name = transparent-apps
  opacity = 0.9 0.6
  match:class = ^(Code|Slack|WebCord|Spotify|Alacritty|foot|footclient)$
}

# Fix some dragging issues with XWayland
windowrule {
  name = xwayland-drag-fix
  no_focus = on
  match:class = ^$
  match:title = ^$
  match:xwayland = 1
  match:float = 1
  match:fullscreen = 0
  match:pin = 0
}

windowrule {
  name = chatterino
  workspace = 19
  tile = on
  match:class = ^(com.chatterino.chatterino)$
}

windowrule {
  name = foot-code-terminal
  workspace = 3
  match:title = ^(foot: Code)$
}

# windowrule = workspace 5,class:^(brave-cifhbcnohmdccbgoicgdjpfamggdegmo-Profile_1)$,initialTitle:Microsoft Teams
windowrule {
  name = jellyfin-desktop
  workspace = 9
  match:class = ^(com.github.iwalton3.jellyfin-media-player)$
}

windowrule {
  name = jellyfin-player-tile
  workspace = 9
  tile = on
  match:class = ^(Jellyfin Media Player)$
}

windowrule {
  name = jellyfin-browser
  workspace = 9
  match:class = ^(Brave-browser)$
  match:title = ^(Jellyfin)$
}

windowrule {
  name = cypress
  workspace = 9
  match:class = ^(Cypress)$
}

windowrule {
  name = vesktop-scratchpad
  workspace = special:magic
  match:class = ^(vesktop)$
}

windowrule {
  name = slack-scratchpad
  workspace = special:magic
  match:class = ^(Slack)$
}


windowrule {
  name = yad-float
  float = on
  match:class = ^(Yad)$
}

windowrule {
  name = galculator-float
  float = on
  match:class = ^(Galculator)$
}

windowrule {
  name = blueberry-float
  float = on
  match:class = ^(Blueberry.py)$
}

windowrule {
  name = notification-toasts-float
  float = on
  match:title = ^(notificationtoasts_.*)$
}

windowrule {
  name = bitwarden-extension-float
  float = on
  match:class = ^(Brave-browser)$
  match:title = ^(_crx_nngceckbapebfimnlniiiahkandclblb)$
}

windowrule {
  name = microsoft-teams
  workspace = 5
  tile = on
  match:initial_title = ^(Microsoft Teams)$
}

windowrule {
  name = pavucontrol-float
  float = on
  match:class = ^(pavucontrol)$
}

windowrule {
  name = qt5ct-float
  float = on
  match:class = ^(qt5ct)$
}

windowrule {
  name = qt6ct-float
  float = on
  match:class = ^(qt6ct)$
}

windowrule {
  name = nwg-look-float
  float = on
  match:class = ^(nwg-look)$
}

windowrule {
  name = bluetooth-sendto-float
  float = on
  match:class = ^(Bluetooth-sendto)$
}

windowrule {
  name = pamac-float
  float = on
  match:class = ^(Pamac-manager)$
}

windowrule {
  name = battlenet-updates-float
  float = on
  match:class = ^(battle.net.exe)$
  match:title = ^(Battle.net Updates)$
}

windowrule {
  name = picture-in-picture
  float = on
  pin = on
  # keep_aspect_ratio = on
  no_initial_focus = on
  # move = ((monitor_w*1)-window_w-20)
  match:title = ^(Picture( |-)in( |-)(p|P)icture)$
}

windowrule {
  name = settings-dialogs-float
  float = on
  match:title = (S|s)ettings
}


windowrule {
  name = osu-fullscreen
  fullscreen = on
  stay_focused = on
  match:class = ^(osu!.exe)$
}


windowrule {
  name = steam-games-fullscreen
  fullscreen = on
  match:initial_class = ^(steam_app_.*)$
}


windowrule {
  name = kwallet5-auth
  pin = on
  center = 1
  workspace = 1
  stay_focused = on
  match:class = ^(kwalletd5)$
}


windowrule {
  name = kwallet6-auth
  pin = on
  workspace = 1
  stay_focused = on
  match:class = ^(org.kde.kwalletd6)$
}


windowrule {
  name = 1password-auth
  pin = on
  workspace = 1
  stay_focused = on
  match:class = ^(1password)$
}


windowrule {
  name = polkit-auth
  float = on
  pin = on
  center = 1
  workspace = 1
  stay_focused = on
  match:class = ^(org.kde.polkit-kde-authentication-agent-1)$
}

windowrule {
  name = pavucontrol-pulseaudio-float
  float = on
  match:class = ^(org.pulseaudio.pavucontrol)$
}


windowrule {
  name = wine-installer-float
  float = on
  match:initial_class = ^(msiexec.exe)$
}


windowrule {
  name = syncthingtray-float
  float = on
  stay_focused = on
  match:class = ^(syncthingtray)$
}

windowrule {
  name = dolphin-new-folder-dialog
  stay_focused = on
  match:initial_title = ^(Create New Folder — Dolphin)$
}

windowrule {
  name = hyprland-polkit-agent
  stay_focused = on
  match:title = ^(Hyprland Polkit Agent)$
}


windowrule {
  name = xwayland-video-bridge-hide
  opacity = 0.0 override
  no_anim = on
  no_initial_focus = on
  max_size = 1 1
  no_blur = on
  match:class = ^(xwaylandvideobridge)$
}


# windowrule {
#   name = espanso-sync-hide
#   opacity = 0.0 override
#   no_anim = on
#   max_size = 20 20
#   no_blur = on
#   float = on
#   workspace = special:hidden
#   match:title = ^(Espanso Sync Tool)$
#   # no_initial_focus = on
# }

windowrule {
  name = dolphin-copy-dialog-float
  float = on
  match:initial_title = ^(Copying — Dolphin)$
}


windowrule {
  name = xdg-portal-gtk
  no_blur = on
  center = 1
  border_size = 0
  no_shadow = on
  match:class = ^(Xdg-desktop-portal-gtk)$
}


windowrule {
  name = satty-screenshot-editor
  float = on
  center = 1
  match:class = ^(com.gabm.satty)
}


windowrule {
  name = discord-popout
  float = on
  pin = on
  match:initial_title = ^(Discord Popout)
}


windowrule {
  name = brave-context-menu-blur-fix
  no_blur = on
  match:class = ^()$
  match:title = ^()$
}


windowrule {
  name = screen-sharing-indicator
  no_anim = on
  no_initial_focus = on
  no_blur = on
  border_size = 0
  no_shadow = on
  opacity = 1 override
  match:title = ^(.*is sharing (your screen|a window).*)$
}

# windowrule = maxsize 1 1, title:^(.*is sharing (your screen|a window).*)$

windowrule {
  name = wine-systray-move
  move = (500) (500)
  match:title = ^(Wine System Tray)$
}

windowrule {
  name = handy-recording-nofocus
  no_initial_focus = on
  border_size = 0
  no_shadow = on
  no_blur = on
  match:class = ^(Handy)$
  match:title = ^(Recording)$
}

# windowrule = opacity 0.0 override 0.0 override, title:^(.*Firefox — Sharing Indicator.*)$
# windowrule = noanim, title:^(.*Firefox — Sharing Indicator.*)$
# windowrule = noinitialfocus, title:^(.*Firefox — Sharing Indicator.*)$
# windowrule = maxsize 1 1, title:^(.*Firefox — Sharing Indicator.*)$
# windowrule = noblur, title:^(.*Firefox — Sharing Indicator.*)$

# blur-background-exclude = [
#   # "window_type = 'dock'",
#   "window_type = 'desktop'",
#   "_GTK_FRAME_EXTENTS@:c",
#   "name *= '-closer'",
#   "class_g = 'slop'"
# ];
#
# layerrule = ignorealpha, gtk-layer-shell
layerrule {
  name = hyprpicker-no-anim
  no_anim = on
  match:namespace = hyprpicker
}

layerrule {
  name = grimblast-selection-no-anim
  no_anim = on
  match:namespace = selection
}

layerrule {
  name = swaync-control-center-blur
  blur = on
  ignore_alpha = 0.5
  match:namespace = swaync-control-center
}

layerrule {
  name = layerrule-gtk-layer-shell
  blur = on
  match:namespace = gtk-layer-shell
}

layerrule {
  name = layerrule-waybar
  blur = on
  match:namespace = waybar
}

layerrule {
  name = swaync-notification-blur
  blur = on
  ignore_alpha = 0.5
  match:namespace = swaync-notification-window
}

layerrule {
  name = logout-dialog-blur
  blur = on
  match:namespace = logout_dialog
}



# layerrule=ignorealpha, swaync-control-center

source = ./hyprland/machine-specific/workspaces.conf
source = ./hyprland/machine-specific/windowrules.conf


###############
### PLUGINS ###
###############
